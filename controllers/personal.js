const { request, response } = require("express");


const mostrarPersonales = (req=request,res=response)=>{

}

const agregarPersonal = (req=request,res=response)=>{

}

const modificarPersonal = (req=request,res=response)=>{
    
}

const eliminarPersonal = (req=request,res=response)=>{
    
}

module.exports = {
    mostrarPersonales,
    agregarPersonal,
    modificarPersonal,
    eliminarPersonal
};